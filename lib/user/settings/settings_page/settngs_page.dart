import 'package:flutter/material.dart';

class SettngsPage extends StatelessWidget {
  const SettngsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}