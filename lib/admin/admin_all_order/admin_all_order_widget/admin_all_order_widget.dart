import 'package:flutter/material.dart';

class AdminAllOrderWidget extends StatelessWidget {
  const AdminAllOrderWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}