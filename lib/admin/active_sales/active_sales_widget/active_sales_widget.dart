
import 'package:flutter/widgets.dart';

// ignore: must_be_immutable
class ActiveSalesWidget  extends StatelessWidget{
  const ActiveSalesWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}