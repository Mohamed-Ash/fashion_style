import 'package:flutter/material.dart';

class AdminReelsWidget extends StatelessWidget {
  const AdminReelsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}