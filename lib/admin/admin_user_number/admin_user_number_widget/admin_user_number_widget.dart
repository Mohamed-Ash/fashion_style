import 'package:flutter/material.dart';

class AdminUserNumberWidget extends StatelessWidget {
  const AdminUserNumberWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}