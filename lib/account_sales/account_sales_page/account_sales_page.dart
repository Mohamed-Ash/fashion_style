import 'package:flutter/material.dart';

class AccountSalesPage extends StatelessWidget {
  const AccountSalesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}